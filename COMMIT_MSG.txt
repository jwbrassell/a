feat: Improve deployment and setup process

- Add setup.py script for one-command initialization
- Update .gitignore to exclude database files but keep static files
- Update README with simplified setup instructions
- Fix RBAC logic to ensure admin role has full access
- Update default admin credentials (admin/test123)
- Add proper route mappings for all plugins
- Consolidate database initialization into single script

This commit makes the application ready for first-time deployment by:
1. Simplifying the setup process to a single command
2. Ensuring proper file tracking in git
3. Fixing role-based access control for admin users
4. Providing clear documentation for new users

Breaking Changes:
- Changed default admin password from 'admin' to 'test123'
- Removed multiple initialization scripts in favor of setup.py
