feat: Add user preferences with dark mode support

- Add UserPreferences model for storing user preferences in JSON format
- Implement dark/light theme toggle in profile plugin
- Add real-time theme switching without page reload
- Add CSRF protection for theme preference updates
- Update base template with dark mode styles and theme toggle
- Add dark mode support for all UI components (tables, forms, cards)
- Update database initialization to create default user preferences
- Add error handling and visual feedback for theme updates
- Update documentation with user preferences features

This change introduces a flexible user preferences system, starting with
dark/light theme support. The preferences are stored as JSON in the database,
making it easy to add more preferences in the future. The theme toggle works
in real-time and persists across sessions.
