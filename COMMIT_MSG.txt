fix: update gitignore to properly track static/dist

- Update gitignore to only ignore root dist/ directory
- Ensure app/static/dist is properly tracked
- Keep AdminLTE files in app/static/dist for proper asset serving
