fix: update gitignore and static file paths

- Remove instance/static and instance/templates exceptions from gitignore
- Update imports.html to use correct paths for AdminLTE files from src directory
- Add all static files to git tracking
